
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
GET         /sendMessage/:action                 controllers.KafkaMessageController.sendMessage(action:String, processId:String?="", taskId:String?="")